<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WinTune.Pages.FastbootModule"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24,16,24,16" RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="12">
            <FontIcon FontSize="22" Glyph="&#xE7BA;" VerticalAlignment="Center" />
            <TextBlock x:Name="HeaderTitle" Style="{StaticResource TitleTextBlockStyle}" />
        </StackPanel>
        <TextBlock x:Name="HeaderBlurb" Grid.Row="1" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />

        <InfoBar x:Name="DangerBar" Grid.Row="2" Severity="Error" IsClosable="False" IsOpen="True" Margin="0,2,0,0" />
        <InfoBar x:Name="EngineBar" Grid.Row="3" IsClosable="False" IsOpen="False" />

        <Grid Grid.Row="4" ColumnSpacing="10" Margin="0,4,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button x:Name="RefreshBtn" Grid.Column="0" Click="Refresh_Click"><FontIcon Glyph="&#xE72C;" FontSize="14" /></Button>
            <ComboBox x:Name="DeviceBox" Grid.Column="1" HorizontalAlignment="Stretch" />
            <CheckBox x:Name="DryRunCheck" Grid.Column="2" IsChecked="True" VerticalAlignment="Center" />
            <ProgressRing x:Name="Busy" Grid.Column="3" IsActive="False" Width="20" Height="20" VerticalAlignment="Center" />
        </Grid>

        <StackPanel Grid.Row="5" Orientation="Horizontal" Spacing="8" Margin="0,2,0,0">
            <Button x:Name="StatusBtn" Click="Status_Click" />
            <Button x:Name="UnlockBtn" Click="Unlock_Click" />
            <Button x:Name="LockBtn" Click="Lock_Click" />
            <Button x:Name="FlashBootBtn" Click="FlashBoot_Click" />
            <Button x:Name="BootBtn" Click="BootImg_Click" />
            <Button x:Name="FactoryBtn" Click="Factory_Click" />
            <Button x:Name="SideloadBtn" Click="Sideload_Click" />
            <Button x:Name="RebootBtn" Click="Reboot_Click" />
        </StackPanel>

        <ScrollViewer Grid.Row="6" VerticalScrollBarVisibility="Auto">
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="12">
                <TextBox x:Name="Console" IsReadOnly="True" AcceptsReturn="True" TextWrapping="NoWrap" FontFamily="Consolas" FontSize="12" BorderThickness="0" Background="Transparent" MinHeight="180" />
            </Border>
        </ScrollViewer>
    </Grid>
</Page>
