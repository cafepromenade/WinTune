<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WinTune.Pages.EventViewerModule"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24,16,24,16" RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="12">
            <FontIcon FontSize="22" Glyph="&#xE7C3;" VerticalAlignment="Center" />
            <TextBlock x:Name="HeaderTitle" Style="{StaticResource TitleTextBlockStyle}" />
        </StackPanel>
        <TextBlock x:Name="HeaderBlurb" Grid.Row="1" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />

        <Grid Grid.Row="2" ColumnSpacing="10" Margin="0,4,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ComboBox x:Name="LogBox" Grid.Column="0" SelectedIndex="0" SelectionChanged="Query_Changed" MinWidth="150" />
            <ComboBox x:Name="LevelBox" Grid.Column="1" SelectedIndex="1" SelectionChanged="Query_Changed" MinWidth="140" />
            <NumberBox x:Name="CountBox" Grid.Column="2" Value="100" Minimum="10" Maximum="500" SpinButtonPlacementMode="Inline" MinWidth="120" ValueChanged="Query_Changed" />
            <TextBox x:Name="FilterBox" Grid.Column="3" TextChanged="Filter_Changed" />
            <ProgressRing x:Name="Busy" Grid.Column="4" IsActive="False" Width="22" Height="22" VerticalAlignment="Center" />
            <Button x:Name="RefreshBtn" Grid.Column="5" Click="Refresh_Click">
                <FontIcon Glyph="&#xE72C;" FontSize="14" />
            </Button>
        </Grid>

        <InfoBar x:Name="HintBar" Grid.Row="3" IsClosable="True" IsOpen="False" Margin="0,2,0,0" />

        <Grid Grid.Row="4" Padding="8,0,8,0" ColumnSpacing="10" Margin="0,2,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150" />
                <ColumnDefinition Width="92" />
                <ColumnDefinition Width="64" />
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="ColTime" Grid.Column="0" FontSize="12" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            <TextBlock x:Name="ColLevel" Grid.Column="1" FontSize="12" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            <TextBlock x:Name="ColId" Grid.Column="2" FontSize="12" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            <TextBlock x:Name="ColProvider" Grid.Column="3" FontSize="12" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            <TextBlock x:Name="ColMessage" Grid.Column="4" FontSize="12" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </Grid>

        <Border Grid.Row="5" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
            <ListView x:Name="List" SelectionMode="Single" SelectionChanged="List_SelectionChanged">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="8,4,8,4" ColumnSpacing="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150" />
                                <ColumnDefinition Width="92" />
                                <ColumnDefinition Width="64" />
                                <ColumnDefinition Width="200" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Time}" VerticalAlignment="Center" FontFamily="Consolas" FontSize="12" />
                            <TextBlock Grid.Column="1" Text="{Binding Level}" VerticalAlignment="Center" FontSize="12" Foreground="{Binding LevelBrush}" />
                            <TextBlock Grid.Column="2" Text="{Binding Id}" VerticalAlignment="Center" FontSize="12" />
                            <TextBlock Grid.Column="3" Text="{Binding Provider}" VerticalAlignment="Center" FontSize="12" TextTrimming="CharacterEllipsis" />
                            <TextBlock Grid.Column="4" Text="{Binding FirstLine}" VerticalAlignment="Center" FontSize="12" TextTrimming="CharacterEllipsis" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

        <Border Grid.Row="6" MinHeight="120" MaxHeight="200" Margin="0,2,0,0" Padding="14,10,14,10"
                Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Spacing="4">
                    <TextBlock x:Name="DetailHead" FontWeight="SemiBold" FontSize="13" TextWrapping="Wrap" />
                    <TextBlock x:Name="DetailBody" FontSize="12" FontFamily="Consolas" TextWrapping="Wrap" IsTextSelectionEnabled="True" />
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Page>
