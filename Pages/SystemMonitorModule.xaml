<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WinTune.Pages.SystemMonitorModule"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24,16,24,16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="12">
                <FontIcon FontSize="22" Glyph="&#xE9D9;" VerticalAlignment="Center" />
                <TextBlock x:Name="HeaderTitle" Style="{StaticResource TitleTextBlockStyle}" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <TextBlock x:Name="SortLabel" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                <ComboBox x:Name="SortBox" SelectionChanged="Sort_Changed" SelectedIndex="0" MinWidth="120" />
            </StackPanel>
        </Grid>
        <TextBlock x:Name="HeaderBlurb" Grid.Row="1" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />

        <ItemsControl Grid.Row="2">
            <StackPanel Orientation="Horizontal" Spacing="12">
                <Border x:Name="CpuCard" Width="220" Padding="16,12,16,12" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                    <StackPanel Spacing="6">
                        <TextBlock x:Name="CpuLabel" FontWeight="SemiBold" />
                        <TextBlock x:Name="CpuValue" FontSize="26" FontWeight="Bold" Text="—" />
                        <Grid>
                            <Border Width="188" Height="6" CornerRadius="3" HorizontalAlignment="Left" Background="{ThemeResource ControlStrongFillColorDefaultBrush}" Opacity="0.25" />
                            <Border x:Name="CpuBar" Width="0" Height="6" CornerRadius="3" HorizontalAlignment="Left" Background="{ThemeResource AccentFillColorDefaultBrush}" />
                        </Grid>
                    </StackPanel>
                </Border>

                <Border x:Name="RamCard" Width="240" Padding="16,12,16,12" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                    <StackPanel Spacing="6">
                        <TextBlock x:Name="RamLabel" FontWeight="SemiBold" />
                        <TextBlock x:Name="RamValue" FontSize="26" FontWeight="Bold" Text="—" />
                        <Grid>
                            <Border Width="208" Height="6" CornerRadius="3" HorizontalAlignment="Left" Background="{ThemeResource ControlStrongFillColorDefaultBrush}" Opacity="0.25" />
                            <Border x:Name="RamBar" Width="0" Height="6" CornerRadius="3" HorizontalAlignment="Left" Background="{ThemeResource AccentFillColorDefaultBrush}" />
                        </Grid>
                        <TextBlock x:Name="RamSub" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    </StackPanel>
                </Border>

                <Border Width="220" Padding="16,12,16,12" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                    <StackPanel Spacing="6">
                        <TextBlock x:Name="NetLabel" FontWeight="SemiBold" />
                        <TextBlock x:Name="NetDown" FontSize="15" Text="↓ —" />
                        <TextBlock x:Name="NetUp" FontSize="15" Text="↑ —" />
                    </StackPanel>
                </Border>

                <Border Width="200" Padding="16,12,16,12" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                    <StackPanel Spacing="6">
                        <TextBlock x:Name="UpLabel" FontWeight="SemiBold" />
                        <TextBlock x:Name="UpValue" FontSize="18" FontWeight="SemiBold" Text="—" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </ItemsControl>

        <Grid Grid.Row="3" Padding="8,0,8,0" ColumnSpacing="10" Margin="0,2,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="64" />
                <ColumnDefinition Width="96" />
                <ColumnDefinition Width="84" />
                <ColumnDefinition Width="40" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="ColName" Grid.Column="0" FontSize="12" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            <TextBlock x:Name="ColCpu" Grid.Column="1" FontSize="12" FontWeight="SemiBold" HorizontalTextAlignment="Right" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            <TextBlock x:Name="ColMem" Grid.Column="2" FontSize="12" FontWeight="SemiBold" HorizontalTextAlignment="Right" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </Grid>

        <Border
            Grid.Row="4"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8">
            <ListView x:Name="ProcList" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="8,5,8,5" ColumnSpacing="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="64" />
                                <ColumnDefinition Width="96" />
                                <ColumnDefinition Width="84" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Name}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                            <TextBlock Grid.Column="1" Text="{Binding CpuText}" VerticalAlignment="Center" HorizontalTextAlignment="Right" />
                            <TextBlock Grid.Column="2" Text="{Binding MemText}" VerticalAlignment="Center" HorizontalTextAlignment="Right" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            <Button Grid.Column="3" Click="Priority_Click" Padding="8,3,8,3" FontSize="12" Content="Priority" ToolTipService.ToolTip="Set priority · 設定優先權" />
                            <Button Grid.Column="4" Click="Kill_Click" Padding="8,3,8,3" ToolTipService.ToolTip="End task · 結束工作"><FontIcon Glyph="&#xE711;" FontSize="12" /></Button>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </Grid>
</Page>
