<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WinTune.Pages.PackageManagerModule"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24,16,24,16" RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="12">
            <FontIcon FontSize="22" Glyph="&#xECAA;" VerticalAlignment="Center" />
            <TextBlock x:Name="HeaderTitle" Style="{StaticResource TitleTextBlockStyle}" />
        </StackPanel>
        <TextBlock x:Name="HeaderBlurb" Grid.Row="1" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />

        <Grid Grid.Row="2" ColumnSpacing="10" Margin="0,4,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <AutoSuggestBox x:Name="SearchBox" Grid.Column="0" QueryIcon="Find" QuerySubmitted="Search_Submitted" />
            <Button x:Name="UpgradesBtn" Grid.Column="1" Click="Upgrades_Click" />
            <ProgressRing x:Name="Busy" Grid.Column="2" IsActive="False" Width="22" Height="22" VerticalAlignment="Center" />
        </Grid>

        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
            <StackPanel Spacing="8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="DepsHeader" FontWeight="SemiBold" FontSize="15" VerticalAlignment="Center" />
                    <Button x:Name="InstallAllBtn" Grid.Column="1" Click="InstallAll_Click" Style="{StaticResource AccentButtonStyle}" />
                </Grid>
                <StackPanel x:Name="DepsPanel" Spacing="6" />

                <TextBlock x:Name="ResultsHeader" FontWeight="SemiBold" FontSize="15" Margin="0,10,0,0" />
                <StackPanel x:Name="ResultsPanel" Spacing="6" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
