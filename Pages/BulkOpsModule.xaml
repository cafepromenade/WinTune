<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WinTune.Pages.BulkOpsModule"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24,16,24,16" RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="12">
            <FontIcon FontSize="22" Glyph="&#xE8B7;" VerticalAlignment="Center" />
            <TextBlock x:Name="HeaderTitle" Style="{StaticResource TitleTextBlockStyle}" />
        </StackPanel>
        <TextBlock x:Name="HeaderBlurb" Grid.Row="1" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />

        <Grid Grid.Row="2" ColumnSpacing="8" Margin="0,4,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="SrcCap" Grid.Column="0" VerticalAlignment="Center" Width="70" />
            <TextBox x:Name="SourceBox" Grid.Column="1" IsReadOnly="True" />
            <Button x:Name="SrcBtn" Grid.Column="2" Click="BrowseSrc_Click" />
        </Grid>

        <Grid Grid.Row="3" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="PatternBox" Grid.Column="0" TextChanged="Input_Changed" />
            <ComboBox x:Name="ModeBox" Grid.Column="1" MinWidth="130" SelectionChanged="Mode_Changed" />
            <CheckBox x:Name="RecurseCheck" Grid.Column="2" Click="Recurse_Changed" />
        </Grid>

        <Border
            Grid.Row="4"
            Margin="0,4,0,0"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8">
            <ListView x:Name="List" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Padding="6,3,6,3" Text="{Binding}" FontFamily="Consolas" FontSize="12" TextTrimming="CharacterEllipsis" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

        <Grid Grid.Row="5" ColumnSpacing="8" Margin="0,4,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="TgtCap" Grid.Column="0" VerticalAlignment="Center" Width="70" />
            <TextBox x:Name="TargetBox" Grid.Column="1" IsReadOnly="True" />
            <Button x:Name="TgtBtn" Grid.Column="2" Click="BrowseTgt_Click" />
        </Grid>

        <StackPanel Grid.Row="6" Orientation="Horizontal" Spacing="8" Margin="0,2,0,0">
            <Button x:Name="CopyBtn" Click="Copy_Click" />
            <Button x:Name="MoveBtn" Click="Move_Click" />
            <Button x:Name="RecycleBtn" Click="Recycle_Click" />
            <Button x:Name="FlattenBtn" Click="Flatten_Click" />
            <Button x:Name="OrganizeBtn" Click="Organize_Click" />
            <TextBlock x:Name="CountText" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel>

        <InfoBar x:Name="ResultBar" Grid.Row="7" IsClosable="True" IsOpen="False" />
    </Grid>
</Page>
