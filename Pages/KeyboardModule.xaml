<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WinTune.Pages.KeyboardModule"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24,16,24,16" RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="12">
            <FontIcon FontSize="22" Glyph="&#xE765;" VerticalAlignment="Center" />
            <TextBlock x:Name="HeaderTitle" Style="{StaticResource TitleTextBlockStyle}" />
        </StackPanel>
        <TextBlock x:Name="HeaderBlurb" Grid.Row="1" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />

        <Grid Grid.Row="2" ColumnSpacing="8" Margin="0,4,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="FromCap" Grid.Column="0" VerticalAlignment="Center" />
            <ComboBox x:Name="FromBox" Grid.Column="1" HorizontalAlignment="Stretch" />
            <FontIcon Grid.Column="2" Glyph="&#xE72A;" FontSize="14" VerticalAlignment="Center" />
            <ComboBox x:Name="ToBox" Grid.Column="3" HorizontalAlignment="Stretch" />
            <Button x:Name="AddBtn" Grid.Column="4" Click="Add_Click" Style="{StaticResource AccentButtonStyle}" />
        </Grid>

        <Border
            Grid.Row="3"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8">
            <ListView x:Name="List" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="8,6,8,6" ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding SourceName}" VerticalAlignment="Center" HorizontalTextAlignment="Right" />
                            <FontIcon Grid.Column="1" Glyph="&#xE72A;" FontSize="12" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                            <TextBlock Grid.Column="2" Text="{Binding TargetName}" VerticalAlignment="Center" FontWeight="SemiBold" />
                            <Button Grid.Column="3" Click="Remove_Click" Padding="8,4,8,4"><FontIcon Glyph="&#xE74D;" FontSize="12" /></Button>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Spacing="8" Margin="0,2,0,0">
            <Button x:Name="ApplyBtn" Click="Apply_Click" Style="{StaticResource AccentButtonStyle}" />
            <Button x:Name="ClearBtn" Click="Clear_Click" />
            <TextBlock x:Name="CountText" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel>

        <InfoBar x:Name="ResultBar" Grid.Row="5" IsClosable="True" IsOpen="False" />
    </Grid>
</Page>
